# Guia de Contribuição

Para manter o projeto organizado, siga o fluxo de trabalho abaixo antes de enviar qualquer alteração.

## 📌 Fluxo de Branches

1. Crie uma branch a partir da `develop` para a sua tarefa:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/nome-da-sua-tarefa